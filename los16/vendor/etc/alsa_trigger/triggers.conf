# Add additional triggers with format "TRIGGER[script_file_name]=The text that will trigger this script" without quotes.
# Script must be placed within /vendor/bin/alsa_trigger/ path.

TRIGGER[headset_plugged.sh]=QuiescentSpeakerRoute: Processing message CONNECT_WIRED_HEADSET
TRIGGER[headset_unplugged.sh]=QuiescentHeadsetRoute: Processing message DISCONNECT_WIRED_HEADSET
